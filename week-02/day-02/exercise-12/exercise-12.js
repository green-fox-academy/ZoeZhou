'use strict';
var height = 100;
var length = 20;
var width = 30;
var surfaceArea = length * width * 2 + length * height * 2 + width * height * 2;
var volume = length * width * height;

console.log('Surfage Area: ' + surfaceArea);
console.log('Volume Area: ' + volume);